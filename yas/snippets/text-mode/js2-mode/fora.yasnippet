# -*- mode: snippet -*-
#contributor : Tolokoban
# name: fora -> for (i=0 ; i<arguments.length ; ...
# --
var ${1:i}, ${2:arg};
for ($1 = 0 ; $1 < arguments.length ; $1++) {
    $2 = arguments[$1];
    $0
}
